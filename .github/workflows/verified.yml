# Refreshes the dsh-doctor verified registry and one badge per declared repo.
# The registry is the only source of truth for badges; a repo that stops
# passing the static R+K gates turns red on the next run instead of keeping a
# stale green badge.
name: verified badges

on:
  push:
    branches: [main]
    paths:
      - 'data/verified-repos.json'
      - 'scripts/**'
      - 'doctor.mjs'
      - 'lib/**'
      - '.github/workflows/verified.yml'
  schedule:
    - cron: '41 5 * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: verified-badges
  cancel-in-progress: false

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Verify declared repos (static R+K)
        run: node scripts/verify.mjs
      - name: Commit registry and badges
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          if [ -n "$(git status --porcelain data badges)" ]; then
            git add data/verified.json badges
            git commit -m 'chore: refresh verified badges'
            git push
          else
            echo 'verified registry unchanged'
          fi
